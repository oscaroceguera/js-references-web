import React from 'react';
import { Container, Title } from './styles';

const Header: React.FC = () => {
  return (
    <Container>
      <Title>
        <span>JSR</span>eferences
      </Title>
    </Container>
  );
};

export default Header;
